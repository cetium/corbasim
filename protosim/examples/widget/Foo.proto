message Foo {

    enum MyEnum {
        HI = 0;
        BYE = 2;
    }

    message Bar {
        required double dou = 1;
        optional string text = 2;
    }

    optional string text = 1;
    repeated string text_repeated = 2;

    repeated Bar bars = 3;

    required MyEnum ee = 4;
}

